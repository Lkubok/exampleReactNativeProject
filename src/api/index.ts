import endpoints from './endpoints';

import { requests as settingsRequests } from './settings';
import { requests as userRequests } from './user';

export { endpoints, settingsRequests, userRequests };
